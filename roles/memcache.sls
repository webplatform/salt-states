# Role to expose Memcached server internally
# We need at least one per cluster, typically called memcache-alpha1
# Make sure you update entries in pillar/production.sls or pillar/staging.sls at alpha_memcache

