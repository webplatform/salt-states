
# Role we use to run webat25.org ExpressionEngine site
# The site has been archived as static site.
# This role still exist in case we need to run the original site from the CMS
#
# To use it, you’ll need to have a VM that has 'webat' it its name
#
# This role will most likely be removed in a foreseeable future.
# see: https://github.com/webplatform/ops/issues/170

include:
  - code.web25ee

