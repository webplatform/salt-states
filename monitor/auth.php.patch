+++ auth.php	2013-08-20 13:48:02.112216471 +0000
@@ -10,6 +10,7 @@
 function embarrassed ()
 {
    global $gmetad_root;
+   $private = array(); // Patch by renoirb
    # The @ in front of a function name suppresses any warnings from it.
    $fp=@fopen("./private_clusters","r");
    if ($fp) {
